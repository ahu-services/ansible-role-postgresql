[defaults]
# Force Ansible to use a dedicated area under /var/tmp so both root and the
# asudo test user share a stable, writable location even if /tmp is remounted
# oddly inside the Molecule Docker containers.
remote_tmp = /var/tmp/ansible-tmp
collections_path = ./.cache/collections:~/.ansible/collections:/usr/share/ansible/collections
